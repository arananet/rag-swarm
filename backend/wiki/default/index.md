# Wiki Index
*Collection: `default` — 4 pages — last updated 2026-04-28 17:53 UTC*

| Page | Summary |
|------|----------|
| [[bert]] | BERT is a neural network-based algorithm for generating text embeddings. It takes in a dataset and produces a vector rep |
| [[embeddings]] | Embeddings are dense numerical vector representations of data that capture semantic meaning in a continuous vector space |
| [[glove]] | GloVe is a neural network-based algorithm for generating text embeddings. It takes in a dataset and produces a vector re |
| [[word2vec]] | Word2Vec is a neural network-based algorithm for generating text embeddings. It takes in a dataset and produces a vector |
